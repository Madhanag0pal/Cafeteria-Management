<!DOCTYPE html>
<html>
  <head>
    <title>CafeteriaManagement</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all", 'data-turbolinks-track': "reload" %>
    <%= javascript_pack_tag "application", 'data-turbolinks-track': "reload" %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary px-3 sticky-top">
      <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">
          <% # Home icon %>
          <svg fill="#333" viewBox="0 0 24 24" width="24px" height="24px">
            <path d="M 12 2 A 1 1 0 0 0 11.289062 2.296875 L 1.203125 11.097656 A 0.5 0.5 0 0 0 1 11.5 A 0.5 0.5 0 0 0 1.5 12 L 4 12 L 4 20 C 4 20.552 4.448 21 5 21 L 9 21 C 9.552 21 10 20.552 10 20 L 10 14 L 14 14 L 14 20 C 14 20.552 14.448 21 15 21 L 19 21 C 19.552 21 20 20.552 20 20 L 20 12 L 22.5 12 A 0.5 0.5 0 0 0 23 11.5 A 0.5 0.5 0 0 0 22.796875 11.097656 L 12.716797 2.3027344 A 1 1 0 0 0 12.710938 2.296875 A 1 1 0 0 0 12 2 z"/>
          </svg>
        </a>

        <div class="collapse navbar-collapse flex-row d-inline-flex px-3" id="navbarToggler">
          <ul class="navbar-nav flex-row flex-wrap bd-navbar-nav pt-2 py-md-0">
            <% MenuCategory.all.each do |catagory| %>
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href=<%= "#" + catagory.name %> ><%= catagory.name %></a>
              </li>
            <% end %>
          </ul>

          <% if @current_user %>
            <ul class="navbar-nav flex-row flex-wrap ms-md-auto">
              <li class="nav-item">
                <%= @current_user.name %>
              </li>
            </ul>
          <% end %>
        </div>

        <div class="d-flex me-4">
          <input class="form-control me-2" type="search" placeholder="Search for Menu Item" aria-label="Search">
          <button class="btn btn-outline-light border-2 " type="submit">Search</button>
        </div>


        <% unless @current_user %>
          <div class="d-inline-flex">
            <button type="button" class="btn btn-success mx-3" data-bs-toggle="modal" data-bs-target="#sign-in-Modal"> Sign In </button>
          </div>
        <% end %>
      </div>
    </nav>

    <!-- Sign In Modal -->
    <div class="modal fade" id="sign-in-Modal" tabindex="-1" aria-labelledby="signInModalLabel" aria-hidden="true">
      <%= render template: "sessions/new" %>
    </div>

    <!-- Sign Up Modal -->
    <div class="modal fade" id="sign-up-Modal" tabindex="-1" aria-labelledby="signUpModalLabel" aria-hidden="true">
      <%= render template: "users/new" %>
    </div>

    <%= yield %>

  </body>
</html>
