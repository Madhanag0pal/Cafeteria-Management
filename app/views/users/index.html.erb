<%= render partial: "list", locals: { title: "Super Admin", users: [User.admin.first], editable: false } %>

<%= render partial: "list", locals: { title: "Admin", users: User.admin[1..], editable: true } %>

<%= render partial: "list", locals: { title: "Clerk", users: User.clerk, editable: true } %>

<%= render partial: "list", locals: { title: "User", users: User.customer, editable: false } %>
