<%= render partial: "list", locals: { title: "Super Admin", users: User.admin.where(id: 1), editable: false } %>

<%= render partial: "list", locals: { title: "Admin", users: User.admin.where.not(id: 1), editable: @current_user.id == 1 } %>

<%= render partial: "list", locals: { title: "Clerk", users: User.clerk, editable: true } %>

<%= render partial: "list", locals: { title: "User", users: User.customer, editable: false } %>
